/**
 * @company name : Eight25Media
 * @auther : Tharindu Wickramasinghe
 * @date : 10/04/2018
 * @description : Logo slider javascript scripts.
 */

(function ($, window, document) {
    $(document).ready(function () {
        /*
        $('.base-scroll-text').each(function () {
            var content_height = $(this).attr('data-content-height');
            var scroll_height = $(this).attr('data-scroll-height');

            //Text block content area
            var scroll_content = $(this).find('.base-text-block__inner');
            //Text block scroll box
            var scroll_bar = $(this).find('.base-text-scroll');
            //Text block scroll content height
            var scroll_content_height = scroll_content.scrollHeight;

            if (scroll_content_height > content_height) {
                scroll_bar.style.display = 'block';
            }
            console.log(content_height);
            console.log(scroll_height);
        });
        */

    })
}(window.jQuery, window, document));